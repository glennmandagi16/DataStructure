def stack_count(stack):
    """
    Returns the number of elements in the stack.

    Pre:
        stack is a pointer to a valid stack.

    Post:
        Stack count is returned.
    """

    return stack.count