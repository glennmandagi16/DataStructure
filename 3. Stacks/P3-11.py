def empty_stack(stack):
    """
    Determines if a stack is empty.

    Pre:
        stack is a valid Stack object.

    Post:
        Returns True if stack is empty.
        Returns False if stack contains data.
    """

    return stack.count == 0